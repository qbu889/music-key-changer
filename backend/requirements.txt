fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
librosa>=0.10
resampy>=0.2.2
soundfile>=0.12
numpy>=1.24
scipy>=1.7

# Source separation (Demucs) for vocal-preserving pitch shift — added 2026-09-04.
# demucs pulls in torch; diffq is required to dequantize the mdx_q model.
demucs>=4.1.0
diffq>=0.2.4
torch>=2.1

# dev / tests
pytest>=8.0
